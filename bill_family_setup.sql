-- เชื่อมโยง "บิลหลัก" (เลขอ้างอิงภายในตอนยังไม่เปิดบิล) กับบิลจริงที่แยกออกไป
-- เมื่อเปิดบิลบางส่วน/เต็มจำนวน — origin_bill_no เก็บเลขอ้างอิงเดิมไว้ทั้งฝั่ง
-- ที่เปิดบิลแล้วและฝั่งที่เหลือยังไม่เปิด ให้ตามกลุ่มกันได้แม้แยกไปหลายรอบ
ALTER TABLE transactions ADD COLUMN IF NOT EXISTS origin_bill_no TEXT;
